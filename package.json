{
  "name": "levelup-fitness-app",
  "version": "1.0.0",
  "description": "Comprehensive fitness tracking app with AI coaching",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd backend && python server.py\" \"cd frontend && yarn start\"",
    "start-backend": "cd backend && python server.py",
    "start-frontend": "cd frontend && yarn start",
    "install-all": "cd frontend && yarn install && cd ../backend && pip install -r requirements.txt"
  },
  "keywords": ["fitness", "ai", "health", "scanning", "gamification"],
  "author": "LevelUP Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}